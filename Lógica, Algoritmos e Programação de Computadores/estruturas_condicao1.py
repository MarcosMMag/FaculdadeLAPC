# A estrutura IF avalia a expressão e se for verdadeira, executa o código.
# Caso contrario o ELSE é executado quando o IF é falso.

# Variavel temperatura
temperatura = 29

#Função IF/ELSE
if temperatura < 30:
    print("A temperatura", temperatura, "° hoje está amena")
else:
    print("Hoje está fazendo calor.")